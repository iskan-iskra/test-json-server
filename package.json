{
  "main": "server.js",
  "scripts": {
    "postinstall": "prisma generate",
    "start": "prisma generate && prisma db push && node api/index.js"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "@vercel/postgres": "^0.8.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "prisma": "^5.14.0"
  }
}
