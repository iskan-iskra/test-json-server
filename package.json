{
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "vercel-build": "prisma generate && prisma migrate deploy && npm run build",
    "build": "echo 'No build step required for this project1'"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "@vercel/postgres": "^0.8.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "prisma": "^5.14.0"
  }
}
